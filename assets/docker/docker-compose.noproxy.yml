version: "2"
services:
  web:
    environment:
      - VIRTUAL_HOST=dkan.local
      - no_proxy=localhost
      #- VIRTUAL_PROTO=https
  cli:
    environment:
      - PHP_IDE_CONFIG=serverName=dkan.local
      - VIRTUAL_HOST=dkan.local
